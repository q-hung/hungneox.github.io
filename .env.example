# Baked in at `npm run build`. For GitHub Pages, also set the same name in:
# Repo → Settings → Secrets and variables → Actions → Variables → PUBLIC_DISQUS_SHORTNAME
#
# If CI has no variables, set DISQUS_SHORTNAME_FALLBACK in src/config/disqus.ts instead.
PUBLIC_DISQUS_SHORTNAME=
